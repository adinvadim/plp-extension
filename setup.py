import os
from setuptools import setup, find_packages

setup(
    name='plp-extension',
    version='0.1',
    packages=find_packages(),
    description='PLP extension',
    url='http://example.com',
    author='author',
)